var expect = chai.expect;

let exerciseName = `TEMPLATE`
describe(`${exerciseName}()`, function() {
  it('should be a function', function() {
    return expect(exerciseName).to.be.a('function');
  });

  
});